# Vista Contacto AmiDog

## Vista rápida

Abrir `pages/contacto.html` con Live Server.

## Editar Sass

```bash
npm install
npm run sass:watch
```

El CSS compilado queda en `styles/css/contacto.css`, por lo que la página también funciona sin instalar dependencias.
